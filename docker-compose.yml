version: '3'
services:
  backend:
    build: ./server
    # container_name: server
    restart: unless-stopped
    networks:
      - fileoptimizer-network
    ports:
      - "5000:5000"
  frontend:
    build: ./web
    # container_name: web
    restart: unless-stopped
    networks:
      - fileoptimizer-network
    ports:
      - "3000:3000"
  nginx:
    build:
      context: ./nginx
    ports:
      - "8080:80"
    depends_on:
      - backend
      - frontend
    networks:
      - fileoptimizer-network

networks:
  fileoptimizer-network :
    driver: bridge
